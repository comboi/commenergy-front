# Canonical frontend API base URL used by lib/api-client.ts
NEXT_PUBLIC_API_URL=http://localhost:5500

# Backwards-compatible fallback for older setups.
# Prefer NEXT_PUBLIC_API_URL for new environments and remove this once no longer needed.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:5500
